# ---------------------------------------------------------------------------
# mymap — Martin: הגשת אריחים וקטוריים מ-PostGIS
# DATABASE_URL מוזרק כמשתנה סביבה מ-docker-compose.yml
# ---------------------------------------------------------------------------

postgres:
  connection_string: ${DATABASE_URL}
  auto_publish: true            # פרסום אוטומטי של כל הטבלאות/הפונקציות המרחביות
